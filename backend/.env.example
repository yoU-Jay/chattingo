# Chattingo Backend Environment Configuration
# Copy this file to .env and update the values

# JWT Configuration (REQUIRED)
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=z9iomEpv/QtMfd2HTXnLzGt/7+R7I38m9k0T4L7GJa0=

# Database Configuration
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/chattingo_db?createDatabaseIfNotExist=true
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=test@123

# CORS Configuration (REQUIRED for frontend communication)
CORS_ALLOWED_ORIGINS=http://52.58.3.148:3000,http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS,PATCH
CORS_ALLOWED_HEADERS=*

# Application Configuration
SPRING_PROFILES_ACTIVE=development
SERVER_PORT=8080

# WebSocket Configuration
WEBSOCKET_ENDPOINT=/ws

# Production Database (for Docker/VPS deployment)
# MYSQL_ROOT_PASSWORD=
# MYSQL_DATABASE=chattingo_db
